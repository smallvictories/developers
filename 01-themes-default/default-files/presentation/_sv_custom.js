!function($) {
  // Disable the Fit Text Plugin
  // window.fitText = true;

  // Customise the carousel
  // For full documentation
  // see http://kenwheeler.github.io/slick/
  window.slickOptions = {
    //vertical: true,
    //infinite: false,
    //verticalSwiping: true,
    //verticalScrolling: true
  }

  // Write your own javascript here.
}(window.jQuery);

# Right-aligned images

## You can right align an image by adding `right` to its filename. You may also want to force it to be 50% of the layout (so it matches the text column), which you can do by adding `half` to the filename.
